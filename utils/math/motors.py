from components.motor.Talon5533 import Talon5533
from subsystems.drive import DriveSubSystem
from math import pi

from utils.math.Vector import Vector


def drive_to_meters(drive, meters: float):
    # drive.set_mode(0)
    # drive.set_position(0)
    # rotations = (6 * pi) * meters
    # drive.move(Vector(0, rotations, 0))
    pass
